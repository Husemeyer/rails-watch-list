<div class="container">
  <div class="row text-center">
    <div class="col-md-12">
      <h1>Movies to watch under genre: <%= @list.name %></h1>
        <% @list.bookmarks.each do |bookmark| %>
          <%= bookmark.movie.title %>
          <%= bookmark.comment %>
        <% end %>
    </div>
  </div>
</div>
